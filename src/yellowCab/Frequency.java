package yellowCab;
import java.util.TimerTask;

public class Frequency extends TimerTask {
	@Override
	public void run() { 
		try
		{
			/*
			 * MyListener myListener = new MyListener();
			 * 
			 * for(int i = 0; i < 31; i++) { for(int j = 0; j < 24; j++) {
			 * if(myListener.dateTime[i][j] != 0) { System.out.println("Frequency on date -"
			 * + myListener.date.split(" ")[0] + " time - "+ myListener.date.split(" ")[1] +
			 * ": "+ myListener.dateTime[i][j]); } } }
			 */
		}
		catch(Exception e)
		{

		}
	}
}
